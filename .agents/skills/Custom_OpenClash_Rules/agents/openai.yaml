interface:
  display_name: "Custom OpenClash Rules"
  short_description: "Repo-specific patterns and workflows for Custom_OpenClash_Rules"
  default_prompt: "Use the Custom_OpenClash_Rules repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true